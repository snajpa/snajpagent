# Default-deny: only the audited command tool may run.
[rule allow-commands]
match  = {"/tool":"^exec_command$"}
action = allow

[rule deny-everything-else]
action  = deny
message = "Only the audited command tool is permitted here."
