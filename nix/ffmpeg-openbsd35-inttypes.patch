# SPDX-License-Identifier: LGPL-2.1-or-later
# Keep old system types and supply missing format macros through one compatibility include.
--- a/configure
+++ b/configure
@@ -7995,6 +7995,10 @@
     'static_assert(__STDC_VERSION_STDBIT_H__ >= 202311L, "Compiler lacks stdbit.h")' || \
     add_cppflags '-I\$(SRC_PATH)/compat/stdbit'
 
+# Some old BSD inttypes.h headers provide types without format macros.
+test_cpp_condition inttypes.h 'defined(PRId64) && defined(SCNu32)' ||
+    add_cppflags '-I\$(SRC_PATH)/compat/inttypes'
+
 # Check if requested libraries were found.
 for lib in $AUTODETECT_LIBS; do
     requested $lib && ! enabled $lib && die "ERROR: $lib requested but not found";
--- /dev/null
+++ b/compat/inttypes/inttypes.h
@@ -0,0 +1,36 @@
+/* SPDX-License-Identifier: LGPL-2.1-or-later */
+#ifndef COMPAT_INTTYPES_H
+#define COMPAT_INTTYPES_H
+
+#include_next <inttypes.h>
+
+/* Early OpenBSD inttypes.h defines types but omits C99 format macros. */
+#if defined(__OpenBSD__) && defined(__clang__) && !defined(PRId64)
+#define PRId8 __INT8_FMTd__
+#define PRIi8 __INT8_FMTi__
+#define PRIu8 __UINT8_FMTu__
+#define PRIx8 __UINT8_FMTx__
+#define PRId16 __INT16_FMTd__
+#define PRIi16 __INT16_FMTi__
+#define PRIu16 __UINT16_FMTu__
+#define PRIx16 __UINT16_FMTx__
+#define PRIX16 __UINT16_FMTX__
+#define PRId32 __INT32_FMTd__
+#define PRIi32 __INT32_FMTi__
+#define PRIu32 __UINT32_FMTu__
+#define PRIx32 __UINT32_FMTx__
+#define PRIX32 __UINT32_FMTX__
+#define PRId64 __INT64_FMTd__
+#define PRIi64 __INT64_FMTi__
+#define PRIu64 __UINT64_FMTu__
+#define PRIx64 __UINT64_FMTx__
+#define PRIX64 __UINT64_FMTX__
+#define PRIdPTR __INTPTR_FMTd__
+#define SCNd32 __INT32_FMTd__
+#define SCNd64 __INT64_FMTd__
+#define SCNu32 __UINT32_FMTu__
+#define SCNu64 __UINT64_FMTu__
+#define SCNx64 __UINT64_FMTx__
+#endif
+
+#endif /* COMPAT_INTTYPES_H */
